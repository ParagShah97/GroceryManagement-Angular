import { SearchProductList } from './search-product-list';

describe('SearchProductList', () => {
  it('should create an instance', () => {
    expect(new SearchProductList()).toBeTruthy();
  });
});
